package io.github.gandrade.groovy.sandbox

interface Greeter {

	/**
	 * By default, methods in interfaces are always public. Otherwise a compilation error will be throw
	 */
	void greet(String name)
}
